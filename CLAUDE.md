# Agent guide

The canonical guide to using and extending this trie library lives in
[`AGENTS.md`](AGENTS.md) (shared with non-Claude agent tools). It is imported
below so Claude Code loads it automatically.

@AGENTS.md
